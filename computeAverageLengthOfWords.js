function computeAverageLengthOfWords(word1, word2) {

    var total = word1.length + word2.length;
    average = total/2;
    return average;
}

/* crea una función que calcule el promedio de dos palabras {

el total será igual a la extensión de la primera palabra más la extensión de la segunda;
el promedio (average) será igual al total dividido en dos;
devuélveme el resultado de average;
}

*/