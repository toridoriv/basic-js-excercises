function convertDoubleSpaceToSingle(str) {
    
    var corrected = str.split("  ").join(" ");
    return corrected;
}

/* 

crea una función que convierta cualquier espacio doble dentro de un string y conviertelo en un espacio simple (ingresa string) {
    
    crea una variable llamada corrected que sea igual al string cuyos dobles espacios han sido cortados y vueltos a unir por un simple espacio;
    devuelveme la variable corrected;
}

*/