function addArrayProperty(obj,key,arr) {

    obj[key] = arr;
    return obj[key];
}

/* 
Añade un array como nueva propiedad dentro de un objeto (objeto,su-propiedad, el-array) {
    la nueva propiedad del objeto será el array que estoy ingresando;
    devuelveme esa nueva propiedad del objeto;
}

*/